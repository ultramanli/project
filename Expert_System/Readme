基于产生式的汽车识别专家系统

车辆集、规则集、特征集、结果集均存放在MySql中，导入sql文件即可；

系统运行的核心思想在于对特征集feature的操作，具体实现在代码的注释中给的较为完善；

当前运行效果略有瑕疵，原因是知识库还不够丰富。

文件说明：
expertsystem_Detail.py:打印推理细节，可以看到对冲突的产生与消解的过程；
expertsystem_NoDetail.py:不打印推理细节，直接看到结果，可以打印出推理成功所使用的产生式；
menu.txt:特征菜单，存放在外部文件，方便程序扩展；
car.sql:车辆集、规则集、特征集、结果集的sql文件，用MySql或其他数据库导入即可
